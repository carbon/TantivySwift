# Binaries are no longer committed (the xcframework ships as a GitHub Release
# asset — see scripts/release.sh), so Git LFS is no longer used. The historical
# 0.1.0 tag still references LFS objects and is unaffected.
